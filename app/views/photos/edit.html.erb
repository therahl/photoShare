<h1>Editing Photo</h1>
<p>
  Photo ID: <%= @photo.id %>
</p>

<%= simple_form_for [@album, @photo] do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.button :submit, "Submit", class: "btn btn-normal" %>
<% end %>

<%= link_to 'Show', album_photo_path %>
<%= link_to 'Back', :back %>
